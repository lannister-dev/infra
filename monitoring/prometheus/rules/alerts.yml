groups:
  - name: infrastructure
    rules:
      - alert: NodeDown
        expr: up{job=~".*node-exporter.*"} == 0
        for: 2m
        labels:
          severity: critical
        annotations:
          summary: "Node exporter down on {{ $labels.instance }}"
          description: "Node exporter has been unreachable for more than 2 minutes."

      - alert: HighCpuUsage
        expr: 100 - (avg by(instance) (rate(node_cpu_seconds_total{mode="idle"}[5m])) * 100) > 85
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "High CPU usage on {{ $labels.instance }}"
          description: "CPU usage has been above 85% for more than 5 minutes (current: {{ $value | printf \"%.1f\" }}%)."

      - alert: HighMemoryUsage
        expr: (1 - node_memory_MemAvailable_bytes / node_memory_MemTotal_bytes) * 100 > 85
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "High memory usage on {{ $labels.instance }}"
          description: "Memory usage has been above 85% for more than 5 minutes (current: {{ $value | printf \"%.1f\" }}%)."

      - alert: HighDiskUsage
        expr: (1 - node_filesystem_avail_bytes{fstype!~"tmpfs|overlay"} / node_filesystem_size_bytes{fstype!~"tmpfs|overlay"}) * 100 > 85
        labels:
          severity: warning
        annotations:
          summary: "High disk usage on {{ $labels.instance }} ({{ $labels.mountpoint }})"
          description: "Disk usage is above 85% (current: {{ $value | printf \"%.1f\" }}%)."

  - name: services
    rules:
      - alert: ServiceDown
        expr: swarm_service_running_tasks == 0 and swarm_service_desired_tasks > 0
        for: 3m
        labels:
          severity: critical
        annotations:
          summary: "Swarm service {{ $labels.service_name }} has 0 running replicas"
          description: "Service has had 0 running tasks for more than 3 minutes."

      - alert: ContainerRestarting
        expr: increase(container_restart_count[10m]) > 3
        labels:
          severity: warning
        annotations:
          summary: "Container {{ $labels.name }} is restarting frequently"
          description: "Container has restarted more than 3 times in the last 10 minutes."

      - alert: CertificateExpiringSoon
        expr: (probe_ssl_earliest_cert_expiry - time()) / 86400 < 7
        labels:
          severity: warning
        annotations:
          summary: "TLS certificate expiring soon on {{ $labels.instance }}"
          description: "Certificate expires in less than 7 days ({{ $value | printf \"%.0f\" }} days remaining)."

      - alert: PrometheusTargetDown
        expr: up == 0
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "Prometheus target {{ $labels.instance }} is down"
          description: "Target has been unreachable for more than 5 minutes."
