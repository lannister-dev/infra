global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:

  - job_name: "services_important"
    dockerswarm_sd_configs:
      - host: unix:///var/run/docker.sock
        role: tasks
    relabel_configs:
      - source_labels: [__meta_dockerswarm_task_desired_state]
        regex: running
        action: keep

      - source_labels: [__meta_dockerswarm_service_label_prometheus_scrape]
        regex: "true"
        action: keep

      - source_labels: [__meta_dockerswarm_service_label_prometheus_srape_profile]
        regex: important
        action: keep

      - source_labels: [__meta_dockerswarm_service_label_prometheus_metrics_port]
        target_label: __address__
        replacement: "$1"