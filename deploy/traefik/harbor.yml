http:
  routers:
    harbor:
      rule: "Host(`registry.lannister-dev.ru`)"
      entryPoints:
        - websecure
      tls:
        certResolver: cloudflare
      service: harbor

  services:
    harbor:
      loadBalancer:
        servers:
          - url: "http://host.docker.internal:8080"