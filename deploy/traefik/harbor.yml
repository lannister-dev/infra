http:
  routers:
    harbor:
      rule: "Host(`harbor.lannister-dev.ru`)"
      entryPoints:
        - websecure
      tls:
        certResolver: cloudflare
      service: harbor

  services:
    harbor:
      loadBalancer:
        servers:
          - url: "http://proxy:8080"