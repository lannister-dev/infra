# Timeweb API Module (Infra Nodes)

Resolves infra node `public_ip` by `server_id` from Timeweb API and returns
normalized `infra_nodes` map.

This module is intended for non-VPN infrastructure nodes (manager/workers).
